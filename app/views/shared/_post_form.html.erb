<%= form_with(model: @post, local: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= f.text_area :content, placeholder: "Crear nuevo post..." %>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>